set -e -x

cd ${BUILD_DIR}/collector
BUNDLE_WITHOUT=development:test bundle package --all --path ./vendor/cache
rm -rf ./vendor/cache/ruby
rm -rf ./vendor/cache/vendor
bundle config --delete NO_INSTALL

